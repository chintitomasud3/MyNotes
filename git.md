**[Response ID: GIT-EXP-2026-001]**

আপনার প্রশ্নটি খুব চমৎকার। ৩০+ বছরের অভিজ্ঞতার আলোতে উত্তর দিচ্ছি—অভিজ্ঞ ডেভেলপাররা কমান্ডের তালিকা মুখস্থ রাখেন না, তারা **সিনারিও (Scenario)** এবং **ঝুঁকি (Risk)** বিশ্লেষণ করে সিদ্ধান্ত নেন।

আমি সাধারণত কোন পরিস্থিতিতে কোন কমান্ডটি ব্যবহার করি, তার একটি প্র্যাকটিকাল গাইডলাইন নিচে দিচ্ছি:

---

### 🎯 আমার ব্যক্তিগত "Go-To" কমান্ডগুলো

| পরিস্থিতি (Scenario) | আমি যেই কমান্ডটি ব্যবহার করি | কেন? |
|---|---|---|
| **ভুলবশত `git add` করে ফেলেছি** | `git restore --staged <file>` (নতুন) <br> অথবা `git reset <file>` (পুরনো) | শুধু স্টেজিং সরাতে চাই, ফাইলের কোডে হাত দেব না। |
| **কমিট মেসেজে টাইপো বা ভুল** | `git commit --amend` | নতুন কমিট না করে, একই কমিটে ঠিক করে নেওয়া যায়। |
| **কোড রেডি, কিন্তু কমিটটা ছিল তাড়াহুড়ো** | `git reset --soft HEAD~1` | কমিট বাতিল, কিন্তু সব কোড স্টেজ করা অবস্থায় ফিরে পাব, আবার কমিট করতে সুবিধা হবে। |
| **লোকালি অনেক এক্সপেরিমেন্ট, এখন সব সাফ** | `git reset --hard origin/main` | লোকাল মেশিনের সব অপ্রয়োজনীয় চেঞ্জ মুছে রিমোটের মতো ফ্রেশ শুরু করতে। |
| **পাবলিক ব্রাঞ্চে ভুল কমিট পুশ করে ফেলেছি** | `git revert <commit-hash>` | **সবচেয়ে নিরাপদ।** ইতিহাস (History) নষ্ট করে না, শুধু ভুলটার প্রভাব বাতিল করে নতুন কমিট তৈরি করে। |

---

### 💼 রিয়েল-লাইফ সিনারিও: একজন সিনিয়র হিসেবে আমার চিন্তাধারা

#### 🔹 সিনারিও ১: "আমি তো স্টেজ করেছি, কিন্তু এখন মনে হচ্ছে এই ফাইলটা এই কমিটে রাখব না"
```bash
# আমি যা করি:
git restore --staged config.php
# অথবা পুরনো স্টাইল:
git reset config.php
```
> **কেন?** কারণ আমি চাই না `config.php` এই কমিটে যুক্ত হোক, কিন্তু ফাইলের ভেতরের কোড ডিলিটও করতে চাই না। এটি নিরাপদ এবং রিভার্সিবল।

#### 🔹 সিনারিও ২: "কমিট করে ফেলেছি, কিন্তু মনে পড়ল ২টা ফাইল অ্যাড করতে ভুলে গেছি"
```bash
# আমি যা করি:
git add forgot_file1.js forgot_file2.js
git commit --amend --no-edit
```
> **কেন?** `reset` করে আবার কমিট করার ঝামেলায় না গিয়ে, একই কমিটের মধ্যে নতুন ফাইলগুলো "সেলাই" করে নিলাম। কমিট হিশ্টরি ক্লিন থাকে।

#### 🔹 সিনারিও ৩: "টিমে কাজ করছি, আমার লোকাল ব্রাঞ্চ একদম গোলমেলে হয়ে গেছে"
```bash
# আমি যা করি (সতর্কতার সাথে):
git fetch origin
git reset --hard origin/develop
```
> **কেন?** আমার লোকাল সব চেঞ্জ বাদ দিয়ে রিমোটের সাথে হুবহু মিলিয়ে নিলাম। এটি খুব শক্তিশালী কমান্ড, তাই ব্যবহারের আগে নিশ্চিত হই যে লোকাল কোনো প্রয়োজনীয় কোড হারাবে না।

---

### 🛡️ একজন অভিজ্ঞ ডেভেলপারের ৩টি সুবর্ণ নিয়ম

১. **পাবলিক ব্রাঞ্চে `git reset --hard` কখনোই নয়!**
   - শেয়ার্ড ব্রাঞ্চে (যেমন: `main`, `develop`) যদি রিসেট করেন, তবে টিমের অন্য সদস্যদের রিপোজিটরি ব্রেক হয়ে যেতে পারে। সেক্ষেত্রে `git revert` ব্যবহার করাই শ্রেয়।

২. **`--hard` ব্যবহারের আগে ৩ বার ভাবুন**
   - এটি চলে গেলে আর ফেরত পাওয়া কঠিন। যদি খুব প্রয়োজন হয়, তবে আগে একটি ব্যাকআপ ব্রাঞ্চ তৈরি করে নিন:
     ```bash
     git branch backup-before-reset
     ```

৩. **টুলস ব্যবহার করুন**
   - সবসময় টার্মিনালে কমান্ড টাইপ করতে হবে এমন কথা নেই। **VS Code**, **GitKraken** বা **Sourcetree**-এর মতো GUI টুলস ব্যবহার করলে ভুল করার সম্ভাবনা কমে এবং ভিজ্যুয়ালি বুঝতে সুবিধা হয়।

---

### 🧠 সারসংক্ষেপ (Cheat Sheet)
- ফাইল আনস্টেজ? → `git reset <file>`
- কমিট মেসেজ ঠিক করা? → `git commit --amend`
- শেষ কমিট বাতিল (কোড রাখতে চাই)? → `git reset --soft HEAD~1`
- লোকাল মেশিন ফ্রেশ করতে চাই? → `git reset --hard origin/branch`
- পাবলিক ব্রাঞ্চে ভুল শুধরাতে চাই? → `git revert`

আপনার যদি কোনো নির্দিষ্ট প্রজেক্ট বা সমস্যা থাকে, শেয়ার করতে পারেন—সেই অনুযায়ী স্পেসিফিক সমাধান দেব! 😊
